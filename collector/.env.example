# Kiwoom REST API credentials - NEVER commit real values
KIWOOM_APP_KEY=
KIWOOM_APP_SECRET=
KIWOOM_BASE_URL=https://api.kiwoom.com

# Google Apps Script Web App endpoint
SHEETS_WEBHOOK_URL=
INGEST_SECRET=

# Existing 3.5 TOP100 collection settings
MARKET_TYPE=000
EXCHANGE_TYPE=3
EXCLUDE_MANAGED=1
POLL_SECONDS=300

# Stage 3 market supply collector
# KRX regular-session investor/program flow, one refresh per minute.
SUPPLY_POLL_SECONDS=60
SUPPLY_ACTIVE_START=08:50
SUPPLY_ACTIVE_END=15:40

# ka10032 trde_prica unit conversion.
# Initial assumption: raw value is KRW million, therefore divide by 100 to convert to 100M KRW (억원).
# MUST be verified against a live sample before production use.
TRADING_VALUE_DIVISOR=100
